## v0.1.4.1 - Auto Match Fix

### Fixed
- Fixed Auto Match counter showing `0 matches` even when mapping rows were visible.
- Fixed mapping display clearing the saved mapping data too early.
- Preserved the existing Dual Preview window structure while adding Auto Match.

### Commit
```bash
git add .
git commit -m "fix: correct auto match mapping"
git push
```
